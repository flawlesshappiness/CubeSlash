using UnityEngine;

[CreateAssetMenu(fileName = nameof(PlayerBodyDatabase), menuName = "Game/" + nameof(PlayerBodyDatabase), order = 1)]
public class PlayerBodyDatabase : Database<PlayerBodyInfo>
{
    
}