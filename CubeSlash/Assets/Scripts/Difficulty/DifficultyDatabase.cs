using UnityEngine;

[CreateAssetMenu(fileName = nameof(DifficultyDatabase), menuName = "Game/" + nameof(DifficultyDatabase), order = 1)]
public class DifficultyDatabase : Database<DifficultyInfo>
{
    
}